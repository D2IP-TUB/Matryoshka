"""Join-discovery indexes compared against the Matryoshka inverted index.

``Aurum``, ``Josie``, ``LSH`` and ``DeepJoin`` each carry their own index
build and query scripts; ``aurum_join_discovery`` wraps Aurum behind the
interface the baseline augmenters expect.
"""
