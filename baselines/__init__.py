"""Baseline augmenters and discovery indexes used in the paper's evaluation.

This package sits outside the installed ``matryoshka`` distribution: it is
present in the repository so that Section 7 can be reproduced, but importing
it is never required to use Matryoshka itself. Run from the repository root,
or add the repository root to ``PYTHONPATH``, so that ``baselines`` resolves.

See ``baselines/README.md`` for the provenance of each component and the
dependencies it needs.
"""
